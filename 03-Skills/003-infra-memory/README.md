# infra-memory（已合併至 deploy-sop）

> ⚠️ 此 skill 已合併至 `001-deploy-docs`（`/deploy-sop`）。
> 伺服器規格、Port 路由表、已知限制都在那裡的「伺服器基礎設施參考」區塊。

## 是什麼

looptw.com 伺服器基礎設施的架構記憶文件（伺服器規格、Port 分配、現有專案路由表）。

## 怎麼用

使用 `/deploy-sop` → 翻到「伺服器基礎設施參考」區塊。

## 原始資料

`source.md`：含完整伺服器架構的原始文件（含敏感資訊，請勿上傳 git）。
